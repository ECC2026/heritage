# Copy this file to application-local.properties and activate the local profile.
# Values below are development placeholders only; do not commit real credentials.
spring.datasource.url=jdbc:mysql://localhost:3306/heritage?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123456

# Use a randomly generated secret with at least 32 characters for real development.
jwt.secret=replace-with-a-random-base64-secret-at-least-32-bytes

spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.database=0

app.upload-dir=uploads
server.port=8080

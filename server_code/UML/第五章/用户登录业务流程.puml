@startuml
start
:用户进入登录页面;
:输入用户名和密码;
:提交登录请求;
if (账号是否存在?) then (是)
  if (密码是否正确?) then (是)
    if (账号状态是否正常?) then (是)
      :生成 Token;
      :返回用户信息;
      :前端保存登录状态;
      :进入系统首页;
    else (否)
      :提示账号被禁用;
    endif
  else (否)
    :提示密码错误;
  endif
else (否)
  :提示账号不存在;
endif
stop
@enduml
# 代码审查修复记录

- 修改时间：2026-08-12 09:46
- 分支：Du
- 基于提交：f8fe687（未重新设计功能，仅做指定修复）

## 修复摘要
- **P0 V003 迁移**：删除误调用 `CALL add_column_if_not_exists('product', 'idx_product_system_id', NULL)`（把索引名当字段名、ddl 为 NULL）。通读全文件确认无其他同类错误。
- **P1 取消预约并发**：cancelBooking 改为原子状态转换 `UPDATE ... SET status=2 WHERE id=? AND user_id=? AND status=1`，仅 `affectedRows==1` 才回补 booked_count；`rows==0` 抛业务异常且不回补。仍为 @Transactional，API 不变。
- **P1 服务状态**：新增 `validateStatus`，create/update/updateStatus 仅允许 0-停用、1-启用，非法值抛业务异常。
- **P1 场次容量**：addSchedule 校验 `capacity >= 0`（0 继续表示不限）。

## 修改文件
- `SQL/migrations/V003__business_ecosystem.sql`
- `server_code/src/main/java/com/example/server_code/service/BusinessServiceBookingService.java`
- `server_code/src/main/java/com/example/server_code/service/BusinessServiceService.java`

## 编译结果
- 后端 `mvn compile`：EXIT=0
- 管理后台 `npm run build`：EXIT=0
- C端 `build:mp-weixin`：EXIT=0

## 运行时验证（MySQL root/1234，8.0.34）
- V003 在临时库完整应用成功：6 个默认体系、product/activity 的 product_system_id 列与索引全部正确。
- cancelBooking 并发：SQL 级模拟两次并发取消，第一次 `ROW_COUNT()=1`（回补执行），第二次 `ROW_COUNT()=0`（不回补），booked_count 只回补一次。
- 测试库已清理，环境恢复原状。

## 未执行的运行时测试
- 完整启动后端做真实 HTTP/多线程并发测试：未执行。原因：`heritage` 库无 ICHIP 基线 schema（无 V001），V002 因缺 `category` 等基线表无法落地；Redis 未运行。

# AGENTS.md

## 1. 项目背景

本项目是“非遗科技人文生态平台”。

项目以 ICHIP 开源项目为基础进行二次开发，包含：

- client_code：UniApp 微信小程序；
- manage_code：Vue 3 运营后台；
- server_code：Spring Boot 后端；
- SQL：数据库脚本；
- UML：原系统设计资料。

第一阶段重点完成微信 C 端小程序业务。

## 2. 第一阶段范围

需要实现：

1. 首页；
2. 非遗传承人专区；
3. 非遗商城；
4. 手作体验与活动预约；
5. 非遗科普知识库；
6. C 端个人中心；
7. 种草分享板块。

暂不实现：

1. AI 虚拟体验专区；
2. AI 数字人；
3. AI 语音问答；
4. AI 知识答题；
5. AI 形象和音色管理。

不得擅自增加 AI 功能。

## 3. 修改原则

1. 每次任务开始前先阅读相关需求文档。
2. 修改前先分析现有实现。
3. 修改前列出实施计划和预计影响文件。
4. 一次只处理一个聚焦任务。
5. 不得修改与当前任务无关的文件。
6. 不得一次性重写整个项目。
7. 优先复用可用代码，但不得为了复用保留明显的安全缺陷。
8. 未经明确批准，不得更换主要技术栈。
9. 未经明确批准，不得新增生产依赖。
10. 修改后必须列出实际执行的检查命令和结果。

## 4. 安全约束

1. 不得将数据库密码、JWT 密钥、微信 AppSecret 或支付密钥写入代码。
2. 敏感配置必须使用环境变量或本地未提交配置。
3. 不得在前端保存 AppSecret。
4. 不得使用明文密码。
5. 不得通过客户端传入的 userId 判断真实登录身份。
6. 管理后台接口必须校验管理员身份和权限。
7. 资质证件不得使用公开静态 URL 无限制访问。
8. 所有订单、支付、退款和核销操作必须支持幂等控制。
9. 禁止在未确认的情况下执行删除数据库或清空数据操作。

## 5. 前端规则

1. 小程序页面不得直接硬编码正式业务数据。
2. 网络请求应通过统一 API 服务层。
3. 页面必须处理：
   - 加载中；
   - 空数据；
   - 网络失败；
   - 登录失效；
   - 重复点击；
   - 接口错误。
4. 公共组件不得在多个页面重复复制。
5. 修改页面后应检查微信小程序兼容性。
6. 不得擅自破坏现有 pages.json 路由。

## 6. 后端规则

1. Controller 只负责参数接收和结果返回。
2. 业务逻辑应放入 Service。
3. 数据访问应放入 Mapper 或 Repository。
4. 订单、库存、活动名额、退款和核销必须使用事务。
5. 修改数据库结构时必须提供迁移 SQL。
6. API 返回结构应保持统一。
7. 新增接口必须说明：
   - 请求方法；
   - 路径；
   - 权限；
   - 请求参数；
   - 返回结构；
   - 错误状态。

## 7. 测试规则

每次修改后，根据影响范围执行：

- 前端构建；
- 后端编译；
- 单元测试；
- 接口测试；
- Git diff 检查。

不得声称未实际运行的测试已经通过。

## 8. 输出格式

完成任务后必须输出：

1. 修改摘要；
2. 修改文件列表；
3. 执行命令；
4. 测试结果；
5. 已知问题；
6. 下一步建议。

## 9. 第一阶段禁止行为

1. 禁止实现 AI 虚拟体验专区。
2. 禁止直接接入真实微信支付，除非任务明确要求。
3. 禁止删除原数据库表。
4. 禁止大规模重命名所有文件。
5. 禁止在没有迁移方案的情况下重构核心数据表。
6. 禁止直接向 main 分支提交。